apiVersion: v1
kind: ResourceQuota
metadata:
  name: strict-quota
  namespace: quota-test
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1"
    limits.memory: "1Gi"
    pods: "5"
    persistentvolumeclaims: "2"
    services.loadbalancers: "0"
