apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: correct-label-monitor
  namespace: default
  labels:
    team: frontend
spec:
  selector:
    matchLabels:
      app: metrics-app
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
